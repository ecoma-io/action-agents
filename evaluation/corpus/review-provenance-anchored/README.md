Exercises provenance anchoring and the digest re-derivation without the verification pass: the recorded conversation reads the one changed file and answers with a single nit, whose anchor the artifact records with the read ledger's line range and sha256 digest. Under the default policy the nit sits on a low-risk file in the shallowest lane, so nothing is planned for verification and the recording ends after the final answer. This entry is where the anchoring-integrity metric re-derives the digest from the recorded head bytes. The snapshot is synthetic, written for this corpus; no real thread, diff or provider transcript is reproduced.
