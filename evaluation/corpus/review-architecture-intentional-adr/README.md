"The twin of review-architecture-introduced-adr-absent with one byte of world difference: the pinned base records the ADR the violation's decision ref reaches, so the prompt's ADR context carries its actual excerpt. The recorded answer weighs that record and classifies the same import as a nit rather than a concern - intentional evolution is a claim with its record, and the record is present. Grounding also lifts the file to the deep lane, so the nit is verified like any deep-lane finding. The snapshot is synthetic, written for this corpus; no real thread, diff or provider transcript is reproduced."
