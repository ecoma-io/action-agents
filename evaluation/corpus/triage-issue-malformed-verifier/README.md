Exercises the verification pass's fail-closed disposition: the assessment
proposes two on-sheet labels, the code-minted plan asks the verifier about
`add:bug` and `add:docs`, and the recorded verify answer is prose no parser
accepts — no retry, no repair, every operation disposes to `uncertain`, every
operation is downgraded, and the run ends `refused` with zero writes. The
record's verification block names both downgrades. The provider cannot
malformed-answer its way past the downgrade-only ceiling; this is the
malformed-provider replay of the triage refusal-rate metric. The snapshot is
synthetic, written for this corpus; no real thread or provider transcript is
reproduced.
