Reproduces the #479-A wrong-anchor nit with the verbatim dogfood text: the model claims duplicate `// CLI entry` header comments but cites the wrong line — line 11 (`record.jobs[jobName] = {};`, a decoy assignment) instead of line 7 where the header first appears; the duplicate header lives at line 19, outside the decoy anchor's ±3 window, exactly as archkeep PR 807's nit cited `:115` while its duplicates sat at `:222`/`:281`. The finding's message carries the quoted span the real nit carried, so the quoted-evidence span gate (#479) withholds it before the verification plan: it is neither published nor scored. The expected pins the correct anchor at line 7 as a true positive and marks a fabricated anchor at line 4 as a distractor that must stay unreported. Modelled on archkeep PR 807 (head c7ad6797). The snapshot is synthetic, written for this corpus; no real thread, diff or provider transcript is reproduced.
