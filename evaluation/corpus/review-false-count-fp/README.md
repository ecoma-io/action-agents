Reproduces the #479-C fixture-count false positive: the model claims the test suite's fixture count differs from a ledger at `docs/ledger.md` (asserting a mismatch against 28 gate fixtures), but no ledger exists anywhere in the synthetic snapshot — `headFiles` holds only the test file and the recorded read never touches `docs/ledger.md`. The cited external reference is fabricated by the model, which is what makes the claim false. The suite itself has exactly 4 `it()` cases, and the expected pins zero findings, so everything the model produces here scores as a false positive. Modelled on loom PR 285 (head 7a1ae04), where the real file held exactly 28 `it(` cases matching the real ledger and the claim of a mismatch was equally false. The snapshot is synthetic, written for this corpus; no real thread, diff or provider transcript is reproduced.
