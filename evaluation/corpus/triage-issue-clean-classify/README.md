Exercises the clean path of a triage run on an issue: the event gate lets an `issues.opened` payload through, the policy sheet resolves from the recorded default-branch tip, the label check passes, the search and issue-form reads come back empty, and the single recorded assessment answer picks one on-sheet label, so the run applies one label and ends `published` with no refusal entry. The snapshot is synthetic: the thread text, the label sheet and the recorded world were written for this corpus, and no real repository thread or provider transcript is reproduced here.
