"Exercises the aware review with architecture evidence whose introduced violation names the changed file: the recipe's delta report and zero-exit manifest sit in headFiles, the constraint row carries a decision ref, and the pinned base records no ADR for it - so the prompt's ADR context line must record the absence rather than invent a record. The recorded answer reads the file, reports the boundary-crossing import as one concern anchored on the evidence's own site, and confirms it under verification. The snapshot is synthetic, written for this corpus; no real thread, diff or provider transcript is reproduced."
