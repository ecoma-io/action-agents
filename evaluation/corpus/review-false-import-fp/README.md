Reproduces the #479-B unused-import false positive: the model claims `readFileSync` (imported at line 3) is dead code, but it is genuinely used at line 7 inside `loadManifest`. The expected pins the usage site at line 7 as a true positive (the anchor a corrected finding should cite) and marks line 1 as a distractor. The produced finding at line 3 — the false claim — is scored as a false positive because no expected finding matches the anchor `check-conformance.test.ts:3`. Modelled on loom PR 285 (head a54b263d). The snapshot is synthetic, written for this corpus; no real thread, diff or provider transcript is reproduced.
