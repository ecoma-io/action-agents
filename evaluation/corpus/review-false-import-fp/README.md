Reproduces the #479-B unused-import false positive with the verbatim dogfood text: the model claims the unused import `readFileSync` (imported at line 3, genuinely used at line 7 inside `loadManifest`) anchors at line 13 — a decoy call site whose ±3 window holds no `readFileSync`, exactly as loom PR 285's claim cited `:9` while the import sat at `:19` and its use at `:489`. The finding's message carries the quoted symbol the real claim carried, so the quoted-evidence span gate (#479) withholds it before the verification plan: it is neither published nor scored. The expected pins the usage site at line 7 as a true positive (the anchor a corrected finding should cite) and marks line 1 as a distractor. Modelled on loom PR 285 (head a54b263d). The snapshot is synthetic, written for this corpus; no real thread, diff or provider transcript is reproduced.
