Exercises the off-sheet ceiling: the recorded assessment names two labels, one of which (`made-up`) is not on the recorded sheet, so the policy engine applies the on-sheet half (`docs`), records a typed refusal entry naming the off-sheet name, applies one label and ends `published`. This entry is one of the two numerator replays of the triage refusal-rate metric. The snapshot is synthetic: the thread text, the label sheet and the recorded world were written for this corpus, and no real repository thread or provider transcript is reproduced here.
