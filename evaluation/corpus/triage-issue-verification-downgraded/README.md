Exercises the opt-in verification pass end to end: the assessment proposes two labels, the code-minted plan asks the verifier about `add:bug` and `add:docs`, the recorded verify answer confirms the first and refutes the second, and the downgrade-only filter turns the refuted op into a typed refusal entry, leaving one applied label and a record whose verification block names the downgrade. This is the other numerator replay of the triage refusal-rate metric. The snapshot is synthetic, written for this corpus; no real thread or provider transcript is reproduced.
