Exercises the opt-in verification pass end to end: the assessment proposes two labels, the code-minted plan asks the verifier about `add:bug`, `add:docs` and the record comment `comment`, the recorded verify answer confirms the first, refutes the second and never judges the third, and the downgrade-only filter turns each unconfirmed op into a typed refusal entry — the unjudged comment disposing `uncertain` like any other — leaving one applied label and a record whose verification block names both downgrades. This is the other numerator replay of the triage refusal-rate metric. The snapshot is synthetic, written for this corpus; no real thread or provider transcript is reproduced.
