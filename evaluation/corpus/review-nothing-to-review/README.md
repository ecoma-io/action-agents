Exercises the empty-universe path: the config records an `ignore` entry that covers the only changed file, so the inventory is empty before any model call. With no prior review comment to clear, the run ends `skip` and writes the durable skip record of kind `nothing-to-review`, and the recording holds no answer file - the zero-ask property is pinned the same way the triage event-skip pins it. The snapshot is synthetic, written for this corpus; no real lockfile or provider transcript is reproduced.
