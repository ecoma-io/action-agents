Synthetic harmonise entry: a workflow-dispatch run with one en source document, no target on disk, no publishing branch, no sync state and no translation memory, and one recorded provider answer proposing the vi translation; the deterministic validation accepts it and the run publishes one branch, one commit and one pull request, pinning the exact Git Data op sequence a clean apply is made of.
