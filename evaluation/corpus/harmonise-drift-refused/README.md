Synthetic harmonise entry: a workflow-dispatch run whose publishing branch carries a sync-state record pinning a translation fingerprint that the on-disk vi document no longer matches, with no translation memory to verify a merge base against; manual-edit protection refuses the pair before any model call, so the entry records zero provider answers and pins the exact deterministic refusal the real validator produces.
