# Archkeep integration

This page lands with its pull request; the skeleton exists so the branch and
the pull request exist before the prose does. It will state the boundary of
the boundary: which of this repository's invariants the pinned archkeep
version actually proves, which sit on the deterministic gates beside it, and
which no static gate can witness.
